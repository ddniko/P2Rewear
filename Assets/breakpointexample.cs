using System;
using UnityEngine;
enum PAGE { RED, GREEN, BLUE }
public class breakpointexample : MonoBehaviour
{
    PAGE page;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
    private void ClosePage(PAGE age)
    {

    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
